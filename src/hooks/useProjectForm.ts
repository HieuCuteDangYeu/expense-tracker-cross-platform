/**
 * Placeholder for useProjectForm hook — will replace ProjectFormViewModel.kt.
 *
 * Responsibilities from Android:
 *   - Form state: name, description, startDate, endDate, manager, budget, status, priority, etc.
 *   - Validation: validateAndSave()
 *   - loadProject(projectId) for edit mode
 *   - saveProject() / updateProject()
 */
export function useProjectForm() {
  // TODO: Implement form state and validation
  return {
    formState: {},
    isLoading: false,
    isSaving: false,
  };
}
