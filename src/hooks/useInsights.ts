/**
 * Placeholder for useInsights hook — will replace InsightsViewModel.kt.
 *
 * Responsibilities from Android:
 *   - expensesByType: List<ExpenseByType> (aggregation)
 *   - totalExpenses, averageExpense
 *   - selectedTimeRange
 */
export function useInsights() {
  // TODO: Implement aggregation queries
  return {
    expensesByType: [],
    totalExpenses: 0,
    isLoading: true,
  };
}
